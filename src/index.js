import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

ReactDOM.render(<App />, document.getElementById("root"));

//want to add new component just fill the data in the emojipedia.js file
//which will automatically create a new card for the data which is put up
